node_modules/
dist/
.vite/
*.log

# Critic gauntlet output — screenshots and perf traces, regenerated every round.
tools/critic/out/

# Raw OSM extracts are large; the baked corridor.json is what ships.
data/*.osm
data/*.pbf
data/*.overpass.json
data/*.geojson

# Alternate build output used by the voucher end-to-end check.
dist-voucher/

# Single-file playable build (npm run bundle).
dist-single/
mukbang-dash.html
